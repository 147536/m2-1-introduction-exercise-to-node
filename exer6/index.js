//Write your own module with a function that returns the length of a string that it receives as a parameter. Import it in the index.js file and show the result on the console.
const length = require("./length");
console.log(length.longitude("Hello World"));
